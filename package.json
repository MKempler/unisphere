{
  "name": "unisphere",
  "version": "0.1.0",
  "private": true,
  "description": "UniSphere - A custodial-key social platform MVP",
  "scripts": {
    "dev": "pnpm --parallel dev",
    "build": "pnpm --recursive build",
    "test": "pnpm --recursive test",
    "lint": "pnpm --recursive lint"
  },
  "engines": {
    "node": ">=16",
    "pnpm": ">=7"
  },
  "packageManager": "pnpm@7.18.0",
  "workspaces": [
    "packages/*"
  ]
} 